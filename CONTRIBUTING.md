# 🤝 المساهمة في المشروع

نشكرك على اهتمامك بالمساهمة! نرحب بجميع أنواع المساهمات.

---

## 📋 جدول المحتويات

- [طرق المساهمة](#طرق-المساهمة)
- [البدء](#البدء)
- [معايير الكود](#معايير-الكود)
- [عملية Pull Request](#عملية-pull-request)
- [الإبلاغ عن مشاكل](#الإبلاغ-عن-مشاكل)
- [ميثاق السلوك](#ميثاق-السلوك)

---

## 🎯 طرق المساهمة

يمكنك المساهمة بعدة طرق:

### 1. 💻 كود
- إضافة ألعاب جديدة
- تحسين الألعاب الموجودة
- إصلاح bugs
- تحسين الأداء
- إضافة tests

### 2. 📝 توثيق
- تحسين README
- إضافة أمثلة
- ترجمة المحتوى
- كتابة tutorials

### 3. 🎨 تصميم
- تحسين Flex Messages
- تصميم أيقونات
- تحسين UX

### 4. 🐛 اختبار
- الإبلاغ عن bugs
- اختبار الميزات الجديدة
- اقتراح تحسينات

### 5. 💡 أفكار
- اقتراح ميزات جديدة
- تحسينات على النظام
- أفكار إبداعية

---

## 🚀 البدء

### 1. Fork المشروع

```bash
# انقر على Fork في GitHub
# ثم استنسخ repo الخاص بك
git clone https://github.com/YOUR_USERNAME/line-bot-arabic.git
cd line-bot-arabic
```

### 2. إعداد البيئة

```bash
# إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# تثبيت المتطلبات
pip install -r requirements.txt

# نسخ ملف البيئة
cp .env.example .env
# عدّل .env بمفاتيحك
```

### 3. إنشاء Branch جديد

```bash
# اسم واضح للميزة
git checkout -b feature/add-puzzle-game

# أو لإصلاح bug
git checkout -b fix/scoring-bug
```

### 4. قم بالتطوير

- اكتب كود نظيف
- اختبر تغييراتك
- اتبع معايير الكود

### 5. Commit التغييرات

```bash
# إضافة الملفات
git add .

# commit برسالة واضحة
git commit -m "✨ Add puzzle game with 3 difficulty levels"
```

### 6. Push والـ Pull Request

```bash
# Push للـ branch
git push origin feature/add-puzzle-game

# اذهب إلى GitHub وافتح Pull Request
```

---

## 📏 معايير الكود

### Python Style

اتبع [PEP 8](https://pep8.org/):

```python
# ✅ جيد
def calculate_score(user_id, points):
    """
    حساب نقاط المستخدم.
    
    Args:
        user_id: معرف المستخدم
        points: النقاط المكتسبة
        
    Returns:
        int: إجمالي النقاط
    """
    user = get_user(user_id)
    return user['score'] + points


# ❌ سيء
def calc(u,p):
    return get_user(u)['score']+p
```

### التسمية

```python
# المتغيرات: snake_case
user_name = "أحمد"
total_score = 100

# الدوال: snake_case
def get_leaderboard():
    pass

# الـ Classes: PascalCase
class GameManager:
    pass

# الثوابت: UPPER_CASE
MAX_TRIES = 3
DEFAULT_TIMEOUT = 5
```

### Docstrings

```python
def my_function(param1, param2):
    """
    وصف مختصر للدالة.
    
    وصف أطول إن لزم الأمر.
    
    Args:
        param1: وصف المعامل الأول
        param2: وصف المعامل الثاني
        
    Returns:
        نوع البيانات: وصف القيمة المرجعة
        
    Raises:
        ExceptionType: متى يحدث الخطأ
    """
    pass
```

### التعليقات

```python
# ✅ تعليقات مفيدة
# التحقق من أن المستخدم مسجل قبل بدء اللعبة
if user_id not in registered_users:
    return False

# ❌ تعليقات واضحة (لا داعي لها)
x = x + 1  # إضافة 1 إلى x
```

### معالجة الأخطاء

```python
# ✅ جيد - معالجة محددة
try:
    result = process_data()
except ValueError as e:
    logger.error(f"قيمة خاطئة: {e}")
    return None
except Exception as e:
    logger.error(f"خطأ غير متوقع: {e}")
    raise

# ❌ سيء - معالجة عامة بدون تسجيل
try:
    result = process_data()
except:
    pass
```

---

## 🔄 عملية Pull Request

### قبل فتح PR

✅ **تأكد من:**

1. الكود يعمل بدون أخطاء
2. تم اختبار جميع الحالات
3. لا توجد ملفات غير ضرورية
4. Commit messages واضحة
5. لا تحتوي على مفاتيح API

### فتح PR

📝 **استخدم القالب:**

```markdown
## الوصف
وصف مختصر للتغييرات

## نوع التغيير
- [ ] ميزة جديدة
- [ ] إصلاح bug
- [ ] تحسين
- [ ] توثيق

## الاختبارات
كيف تم اختبار التغييرات؟

## Screenshots (إن وجدت)
أضف صور للتغييرات البصرية

## Checklist
- [ ] الكود يتبع معايير المشروع
- [ ] تم الاختبار محلياً
- [ ] تم تحديث التوثيق
- [ ] لا توجد تحذيرات
```

### مراجعة الكود

🔍 **عملية المراجعة:**

1. الفريق سيراجع PR
2. قد نطلب تعديلات
3. ناقش أي اقتراحات
4. عدّل حسب التعليقات
5. بعد الموافقة → Merge!

---

## 🐛 الإبلاغ عن مشاكل

### قبل فتح Issue

🔍 **تحقق من:**

1. المشكلة غير موجودة في Issues الحالية
2. جربت الحلول في FAQ
3. لديك معلومات كافية للإبلاغ

### قالب الـ Bug Report

```markdown
## وصف المشكلة
وصف واضح ومختصر للمشكلة

## خطوات إعادة الإنتاج
1. اذهب إلى '...'
2. اضغط على '...'
3. لاحظ الخطأ

## السلوك المتوقع
ماذا كان يجب أن يحدث؟

## السلوك الفعلي
ماذا حدث بالفعل؟

## Screenshots
أضف صور إن وجدت

## البيئة
- نظام التشغيل: [e.g. macOS, Windows]
- Python version: [e.g. 3.11]
- LINE Bot SDK version: [e.g. 3.5.0]

## معلومات إضافية
أي تفاصيل أخرى مفيدة

## Logs
```
الصق رسائل الخطأ هنا
```
```

### قالب الـ Feature Request

```markdown
## الميزة المقترحة
وصف واضح للميزة

## المشكلة التي تحلها
لماذا نحتاج هذه الميزة؟

## الحل المقترح
كيف يمكن تنفيذها؟

## البدائل المتاحة
هل هناك طرق أخرى؟

## معلومات إضافية
أي تفاصيل أخرى
```

---

## 🎮 إضافة لعبة جديدة

### خطوات مفصلة

1. **إنشاء ملف اللعبة**
   ```bash
   touch games/my_game.py
   ```

2. **استخدم القالب**
   ```python
   class MyGame:
       def __init__(self, gemini_helper):
           self.gemini_helper = gemini_helper
           self.tries_left = 3
       
       def generate_question(self):
           # توليد السؤال
           return "السؤال"
       
       def check_answer(self, user_answer):
           # التحقق
           return True/False
       
       def get_correct_answer(self):
           return "الإجابة"
       
       def decrement_tries(self):
           self.tries_left -= 1
           return self.tries_left
   ```

3. **سجّل في app.py**
   ```python
   from games.my_game import MyGame
   
   GAMES = {
       'لعبتي': '🎲'
   }
   
   games_map = {
       'لعبتي': MyGame
   }
   ```

4. **اختبر محلياً**
   ```bash
   python app.py
   # جرّب اللعبة في LINE
   ```

5. **افتح PR**

---

## ✅ Checklist قبل Submit

- [ ] الكود يعمل محلياً بدون أخطاء
- [ ] تم اختبار جميع الحالات
- [ ] اتبعت معايير الكود
- [ ] أضفت docstrings للدوال الجديدة
- [ ] حدّثت التوثيق إن لزم
- [ ] Commit messages واضحة
- [ ] لا توجد ملفات حساسة (.env, keys)
- [ ] Branch محدّث من main

---

## 🎨 إرشادات التصميم

### Flex Messages

```python
# الألوان المستخدمة
COLORS = {
    "primary": "#111111",
    "secondary": "#555555",
    "light": "#999999",
    "white": "#FFFFFF",
    "success": "#00B900"
}

# الأحجام
SIZES = ["xxs", "xs", "sm", "md", "lg", "xl", "xxl"]

# استخدم margin و padding متسقة
"margin": "md"
"paddingAll": "20px"
```

### رسائل المستخدم

- واضحة ومختصرة
- باللغة العربية الفصحى البسيطة
- مشجعة وإيجابية
- تحتوي على إيموجي مناسب

---

## ❓ الأسئلة

### لدي سؤال حول...

**الكود:**
- افتح Discussion على GitHub
- أو اسأل في Issues

**المساهمة:**
- اقرأ هذا الملف
- DEVELOPER_GUIDE.md
- أو اسأل في Discussion

**المشروع:**
- README.md
- FAQ.md

---

## 📬 التواصل

- **GitHub Issues**: للمشاكل والميزات
- **GitHub Discussions**: للأسئلة والنقاش
- **Pull Requests**: للمساهمات

---

## 📜 ميثاق السلوك

### قيمنا

✅ **نحن نرحب بـ:**
- الاحترام المتبادل
- التعاون البنّاء
- التعليقات المفيدة
- الصبر مع المبتدئين

❌ **نحن لا نقبل:**
- التنمر أو المضايقة
- التعليقات المسيئة
- السلوك غير المهني
- أي شكل من أشكال التمييز

### العواقب

انتهاك ميثاق السلوك قد يؤدي إلى:
1. تحذير
2. حظر مؤقت
3. حظر دائم

---

## 🎉 شكراً

شكراً لمساهمتك في تطوير المشروع! 

كل مساهمة، مهما كانت صغيرة، تساعد في تحسين البوت للجميع.

**Contributors سيتم إضافتهم في:**
- README.md
- صفحة Contributors على GitHub

---

## 📚 موارد مفيدة

- [Git Tutorial](https://git-scm.com/docs/gittutorial)
- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Python PEP 8](https://pep8.org/)
- [LINE Bot API](https://developers.line.biz/en/docs/messaging-api/)

---

**آخر تحديث:** ديسمبر 2024

**نسعد بمساهمتك! 🚀**
